import React from "react";
import Annotation from "../presentational/annotation";

const AnnotationBrowser = ({ label, text, type, id, value, handleChange }) => (
    <div className="annotation-browser">
        
    </div>
);

export default Annotation;